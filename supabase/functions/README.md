# Supabase Edge Functions

This folder contains all backend logic for the Askleo application. Each subdirectory corresponds to a single Edge Function deployed to Supabase.

Use the Supabase CLI to develop and deploy functions. Environment variables are loaded from the local `.env` file during development.
